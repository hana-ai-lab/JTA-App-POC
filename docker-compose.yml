services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: jta
      POSTGRES_USER: jta
      POSTGRES_PASSWORD: jta_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
